{
	"name": "cfw-test1",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"preinstall": "pnpm audit",
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"login": "wrangler login",
		"login-no-browser": "wrangler login --browser false",
		"upload": "wrangler versions upload",
		"preview": "wrangler versions upload",
		"release": "wrangler versions deploy",
		"test:watch": "vitest",
		"test": "vitest run",
		"cf-typegen": "wrangler types",
		"size": "wrangler deploy --outdir tmp/bundled/ --dry-run",
		"format": "oxfmt",
		"lint": "oxlint",
		"check": "oxfmt && oxlint --fix"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.18.7",
		"@cloudflare/workers-types": "^5.20260723.1",
		"typescript": "^7.0.2",
		"vitest": "^4.1.10",
		"wrangler": "^4.113.0"
	}
}
