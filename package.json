{
	"name": "cfw-test1",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"preinstall": "pnpm audit",
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"dev:remote": "wrangler dev --remote",
		"upload": "wrangler versions upload",
		"preview": "wrangler versions upload",
		"release": "wrangler versions deploy",
		"test": "vitest",
		"cf-typegen": "wrangler types",
		"size": "wrangler deploy --outdir tmp/bundled/ --dry-run",
		"format": "oxfmt",
		"lint": "oxlint",
		"check": "oxfmt && oxlint --fix"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.16.19",
		"@cloudflare/workers-types": "^4.20260624.1",
		"typescript": "^6.0.3",
		"vitest": "^4.1.9",
		"wrangler": "^4.104.0"
	}
}
